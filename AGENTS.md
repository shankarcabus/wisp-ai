# Agents

See [CLAUDE.md](CLAUDE.md). It carries the project rule (board differences go in
`firmware/main/board.h`, style differences do not exist), the build and flash
commands for both boards, a symptom → cause → fix table of traps that do not
announce themselves, and a list of behaviours that look like bugs and are not.
